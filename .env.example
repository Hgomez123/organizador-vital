DATABASE_URL="postgresql://organizador:organizador@localhost:5432/organizador?schema=public"
# Usuario único (modo personal). Migrar a NextAuth en Fase 2.
DEFAULT_USER_EMAIL="gomezbarrioshermerson@gmail.com"

# OPCIONAL — habilita el agente IA del planificador conversacional.
# Sin esta clave la app funciona igual, con el motor de reglas local.
# Consíguela en https://console.anthropic.com
ANTHROPIC_API_KEY=""

# ── NOTIFICACIONES PUSH ──
# Genera estas cuatro con: npm run push:keys
# Se generan UNA vez; si las cambias, todos los dispositivos deben resuscribirse.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:gomezbarrioshermerson@gmail.com"

# Protege el endpoint que dispara los envíos reales.
CRON_SECRET=""

# URL base para el script de pruebas del cron.
APP_URL="http://localhost:3000"
